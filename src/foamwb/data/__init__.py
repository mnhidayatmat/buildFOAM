"""Version- and lineage-specific data (§3.4, §5.4).

Exempt from ``tools/check_version_literals.py``: the manifest and the dictionary
schemas are *supposed* to name OpenFOAM versions and dictionary filenames. That
is the design — keeping them here is what keeps them out of code.
"""
