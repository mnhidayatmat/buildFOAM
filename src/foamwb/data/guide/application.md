# The application itself

## Update failed

**E-A01.** An update did not complete and was rolled back.

The version you were running has been restored and is intact. Nothing in your
cases is touched by an update. Try again later; if it keeps failing, a proxy or
firewall is the most likely cause.
