"""Application services — pure Python, no Qt (NFR-M1, §4.1).

This subtree must remain importable and testable without a display server, and
without PySide6 installed at all. CI enforces it on every commit.
"""
